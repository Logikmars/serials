const Router = require('express').Router;
const commentController = require('./comment-controller'); 
const router = new Router();

// router.post('/', commentController.set); 
// router.get('/', commentController.get); 

module.exports = router;