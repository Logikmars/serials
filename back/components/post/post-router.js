const Router = require('express').Router;
const postController = require('./post-controller'); 
const router = new Router();

// router.post('/', postController.set); 
// router.get('/', postController.get); 

module.exports = router;