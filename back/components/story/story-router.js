const Router = require('express').Router;
const storyController = require('./story-controller'); 
const router = new Router();

// router.post('/', storyController.set); 
// router.get('/', storyController.get); 

module.exports = router;